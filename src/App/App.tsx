import React from "react";

import RootContainer from "./RootContainer";

const App: React.VFC<{}> = (): JSX.Element => (
  <div className="app-global_container">
    <RootContainer />
  </div>
);

export default App;
